{
  "configurations": [
    {
      "type": "node-terminal",
      "name": "Run Script: dev-server",
      "request": "launch",
      "command": "npm run dev-server",
      "cwd": "${workspaceFolder}"
    }
  ]
}